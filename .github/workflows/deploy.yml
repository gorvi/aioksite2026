name: Deploy to Tencent Cloud

# 📝 注意事项：
# 1. 服务器上必须存在 ecosystem.config.js（包含数据库配置）
# 2. 该文件不在 Git 中（已在 .gitignore），需手动在服务器上配置
# 3. 使用 PM2 管理进程，环境变量在 ecosystem.config.js 中定义
# 4. 部署完成后应用会自动重启并加载最新代码

on:
  push:
    branches:
      - main  # 监听 main 分支的推送
  workflow_dispatch: # 允许手动触发

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }} # 如果使用密码登录
          # key: ${{ secrets.SERVER_SSH_KEY }}     # 如果使用私钥登录，任选其一
          script: |
            git config --global --add safe.directory /www/wwwroot/aioksite2026
            cd /www/wwwroot/aioksite2026
            
            echo "🚚 Pulling latest code..."
            git fetch --all
            git reset --hard origin/main
            chown -R www:www .
            
            echo "📦 Installing dependencies..."
            npm install --production=false
            
            echo "🏗️  Building project (this takes ~2 mins)..."
            npm run build
            
            echo "🔧 Setting correct ownership..."
            chown -R www:www .
            
            echo "🚀 Restarting Service via PM2..."
            # 确保 ecosystem.config.js 存在（如果不存在则从示例创建）
            if [ ! -f "ecosystem.config.js" ]; then
              echo "⚠️  ecosystem.config.js not found, creating from example..."
              cp ecosystem.config.example.js ecosystem.config.js
              echo "❌ Please configure database credentials in ecosystem.config.js"
              exit 1
            fi
            
            # 使用 ecosystem.config.js 重启（包含环境变量配置）
            pm2 restart ecosystem.config.js --update-env || pm2 start ecosystem.config.js
            pm2 save
            
            echo "🧹 Clearing Nginx Cache..."
            rm -rf /www/server/nginx/proxy_cache_dir/*
            /etc/init.d/nginx restart
            
            echo "✅ ALL SYSTEMS GO! Deployment Successful."
